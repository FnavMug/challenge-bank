package com.challenge.bank.service.enums;

/**
 * Enum that indicates the types of transactions that can be
 * fulfilled within the banking system
 */
public enum TransactionType {
    DEPOSIT,
    WITHDRAWAL,
    TRANSFER
}
